# Agents Surface

Skills live in `skills/` — the whole process surface of this repo:

- `state-kernel` — storage kernel usage law and extension protocol
- `ci` — remote verification plane and witness rule

Invariants and pointers live in the root `AGENTS.md`.
