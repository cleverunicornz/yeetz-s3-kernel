{
  "$comment": "Installed by bedrock v0.6.0 — DO NOT EDIT. This file is owned by the tool. Base changes: file an issue or PR at https://github.com/cleverunicornz/bedrock. Local refresh: bedrock update.",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://yeetz.dev/bedrock/schema/definition/v2",
  "title": "Bedrock definition namespace vertex schema",
  "description": "Invariants, breadcrumbs, and terms (SPINE §4 C4 minimum-vertex rule). Validates every situation/definition/*.yamlld vertex.",
  "type": "object",
  "additionalProperties": false,
  "required": ["@context", "@id", "@type"],
  "properties": {
    "@context": { "const": "https://yeetz.dev/bedrock/context/v1" },
    "@id": { "$ref": "#/$defs/vertexId" },
    "@type": { "$ref": "#/$defs/type" },
    "label": { "$ref": "#/$defs/label" },
    "statement": { "$ref": "#/$defs/boundedText" },
    "layer": { "enum": ["floor", "situated"] },
    "gate": { "$ref": "#/$defs/boundedText" },
    "requires": { "$ref": "#/$defs/vertexPointer" },
    "references": { "$ref": "#/$defs/vertexPointerList" },
    "consumes": { "$ref": "#/$defs/pointerList" },
    "produces": { "$ref": "#/$defs/pathPointerList" },
    "member-of": { "$ref": "#/$defs/vertexPointer" },
    "source": { "$ref": "#/$defs/pathPointer" },
    "path": { "$ref": "#/$defs/pathPointerList" },
    "body": { "$ref": "#/$defs/body" }
  },
  "minProperties": 3,
  "allOf": [
    {
      "if": { "anyOf": [
        { "properties": { "@type": { "const": "https://yeetz.dev/bedrock/ontology/Invariant" } } },
        { "properties": { "@type": { "type": "array", "contains": { "const": "https://yeetz.dev/bedrock/ontology/Invariant" } } } }
      ], "required": ["@type"] },
      "then": { "required": ["statement", "layer"] }
    },
    {
      "if": { "anyOf": [
        { "properties": { "@type": { "const": "https://yeetz.dev/bedrock/ontology/Breadcrumb" } } },
        { "properties": { "@type": { "type": "array", "contains": { "const": "https://yeetz.dev/bedrock/ontology/Breadcrumb" } } } }
      ], "required": ["@type"] },
      "then": { "required": ["gate"] }
    },
    {
      "if": { "anyOf": [
        { "properties": { "@type": { "const": "https://yeetz.dev/bedrock/ontology/Term" } } },
        { "properties": { "@type": { "type": "array", "contains": { "const": "https://yeetz.dev/bedrock/ontology/Term" } } } }
      ], "required": ["@type"] },
      "then": { "required": ["statement"] }
    }
  ],
  "anyOf": [{ "required": ["label"] }, { "required": ["statement"] }],
  "$defs": {
    "vertexId": { "type": "string", "pattern": "^https://yeetz\\.dev/bedrock/vertex/[a-z0-9][a-z0-9-]*$" },
    "label": { "type": "string", "minLength": 1, "maxLength": 120 },
    "boundedText": { "type": "string", "minLength": 1, "maxLength": 4096 },
    "body": { "type": "string", "minLength": 1, "$comment": "Unbounded node-local depth; always cold, delivered through the resident vertex's document edge." },
    "pathPointer": { "type": "string", "pattern": "^https://yeetz\\.dev/bedrock/path/[A-Za-z0-9._/\\-]+$" },
    "vertexPointer": { "type": "string", "$comment": "single edge target", "pattern": "^https://yeetz\\.dev/bedrock/vertex/[a-z0-9][a-z0-9-]*$" },
    "vertexPointerList": { "type": "array", "minItems": 1, "uniqueItems": true, "items": { "$ref": "#/$defs/vertexPointer" } },
    "pointer": { "type": "string", "pattern": "^https://yeetz\\.dev/bedrock/(vertex|path)/[A-Za-z0-9._/\\-]+$" },
    "pointerList": { "type": "array", "minItems": 1, "uniqueItems": true, "items": { "$ref": "#/$defs/pointer" } },
    "pathPointerList": { "type": "array", "minItems": 1, "uniqueItems": true, "items": { "$ref": "#/$defs/pathPointer" } },
    "type": {
      "description": "one base type scalar, or an array with at least one base type (repo archetypes ride alongside; rule C9)",
      "oneOf": [
        { "type": "string", "enum": [
          "https://yeetz.dev/bedrock/ontology/Invariant",
          "https://yeetz.dev/bedrock/ontology/Breadcrumb",
          "https://yeetz.dev/bedrock/ontology/Term"
        ] },
        { "type": "array", "minItems": 1, "uniqueItems": true,
          "items": { "type": "string", "pattern": "^https://[^\\s]+$" },
          "contains": { "enum": [
            "https://yeetz.dev/bedrock/ontology/Invariant",
            "https://yeetz.dev/bedrock/ontology/Breadcrumb",
            "https://yeetz.dev/bedrock/ontology/Term"
          ] } }
      ]
    }
  }
}
