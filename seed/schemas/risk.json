{
  "$comment": "Installed by bedrock v0.7.0 — DO NOT EDIT. This file is owned by the tool. Base changes: file an issue or PR at https://github.com/cleverunicornz/bedrock. Local refresh: bedrock update.",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:bedrock:schema/risk/v2",
  "title": "Bedrock risk namespace vertex schema",
  "description": "Present-tense warnings; deleted when retired (SPINE §4 C4 minimum-vertex rule). Validates every situation/risk/*.yamlld vertex.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "@context",
    "@id",
    "@type"
  ],
  "properties": {
    "@context": {
      "enum": [
        "urn:bedrock:context/v1",
        "https://yeetz.dev/bedrock/context/v1"
      ]
    },
    "@id": {
      "$ref": "#/$defs/vertexId"
    },
    "@type": {
      "description": "Risk scalar, or an array containing Risk (repo archetypes ride alongside; rule C9)",
      "oneOf": [
        {
          "enum": [
            "urn:bedrock:ontology/Risk",
            "https://yeetz.dev/bedrock/ontology/Risk"
          ]
        },
        {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": {
            "type": "string",
            "pattern": "^[A-Za-z][A-Za-z0-9+.-]*:[^\\s]+$"
          },
          "contains": {
            "enum": [
              "urn:bedrock:ontology/Risk",
              "https://yeetz.dev/bedrock/ontology/Risk"
            ]
          }
        }
      ]
    },
    "label": {
      "$ref": "#/$defs/label"
    },
    "statement": {
      "$ref": "#/$defs/boundedText"
    },
    "requires": {
      "$ref": "#/$defs/vertexPointer"
    },
    "references": {
      "$ref": "#/$defs/vertexPointerList"
    },
    "consumes": {
      "$ref": "#/$defs/pointerList"
    },
    "produces": {
      "$ref": "#/$defs/pathPointerList"
    },
    "member-of": {
      "$ref": "#/$defs/vertexPointer"
    },
    "source": {
      "$ref": "#/$defs/pathPointer"
    },
    "path": {
      "$ref": "#/$defs/pathPointerList"
    },
    "body": {
      "$ref": "#/$defs/body"
    }
  },
  "minProperties": 3,
  "anyOf": [
    {
      "required": [
        "label"
      ]
    },
    {
      "required": [
        "statement"
      ]
    }
  ],
  "$defs": {
    "vertexId": {
      "type": "string",
      "pattern": "^(?:urn:bedrock:vertex/|https://yeetz\\.dev/bedrock/vertex/)[a-z0-9][a-z0-9-]*$"
    },
    "label": {
      "type": "string",
      "minLength": 1,
      "maxLength": 120
    },
    "boundedText": {
      "type": "string",
      "minLength": 1,
      "maxLength": 4096
    },
    "body": {
      "type": "string",
      "minLength": 1,
      "$comment": "Unbounded node-local depth; always cold, delivered through the resident vertex's document edge."
    },
    "pathPointer": {
      "type": "string",
      "pattern": "^(?:urn:bedrock:path/|https://yeetz\\.dev/bedrock/path/)[A-Za-z0-9._/\\-]+$"
    },
    "vertexPointer": {
      "type": "string",
      "pattern": "^(?:urn:bedrock:vertex/|https://yeetz\\.dev/bedrock/vertex/)[a-z0-9][a-z0-9-]*$"
    },
    "vertexPointerList": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {
        "$ref": "#/$defs/vertexPointer"
      }
    },
    "pointer": {
      "type": "string",
      "pattern": "^(?:urn:bedrock:|https://yeetz\\.dev/bedrock/)(?:vertex|path)/[A-Za-z0-9._/\\-]+$"
    },
    "pointerList": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {
        "$ref": "#/$defs/pointer"
      }
    },
    "pathPointerList": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {
        "$ref": "#/$defs/pathPointer"
      }
    }
  }
}
