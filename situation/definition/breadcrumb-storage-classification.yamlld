# seeded by bedrock v0.2.1
# Breadcrumb: storage classification tooling. Spec/floor-v2.md §4 list.
# Pointer is institutional (ephemeral-test default); it lives in the gate prose.
"@context": "urn:bedrock:context/v1"
"@id": "urn:bedrock:vertex/breadcrumb-storage-classification"
"@type": "urn:bedrock:ontology/Breadcrumb"
label: "Storage classification"
gate: >
  Ephemeral-test default: fresh bucket + scoped key, torn down on exit;
  retention needs a named owner and lifetime.
