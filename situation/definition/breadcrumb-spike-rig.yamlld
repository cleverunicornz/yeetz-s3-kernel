# seeded by bedrock v0.2.1
# Breadcrumb to the Spike Playbook. Spec/floor-v2.md §4 breadcrumb list.
# source points at the playbook path W3 ships in situation/references/;
# the file's absence is tolerated until W3 lands it (playbooks in flight).
"@context": "urn:bedrock:context/v1"
"@id": "urn:bedrock:vertex/breadcrumb-spike-rig"
"@type": "urn:bedrock:ontology/Breadcrumb"
label: "Spike rig"
gate: >
  When a question needs a build: quarantine it, timebox it, predeclare criteria
  before the rig answers them.
source: "urn:bedrock:path/situation/references/spike-playbook.md"
