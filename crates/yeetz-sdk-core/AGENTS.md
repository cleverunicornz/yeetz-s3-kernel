# SDK Core

Source of record (extracted from the yeetz forge) — provider-neutral,
request-scoped SDK primitives composed by `yeetz-sdk-s3`, closure of
the state kernel. Usage law: `.agents/skills/state-kernel`.
