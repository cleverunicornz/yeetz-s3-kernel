# S3 SDK

Source of record (extracted from the yeetz forge) — request-scoped
S3-compatible object storage mechanics, closure of the state kernel.
Usage law: `.agents/skills/state-kernel`. Pins are assured versions;
do not bump casually.
